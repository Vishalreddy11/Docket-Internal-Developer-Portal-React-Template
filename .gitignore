.env
*.log
node_modules/
dist/
dist-ssr/
coverage/
.DS_Store
.vscode/
.idea/
*.local
playwright-report/
test-results/

# Build caches / incremental build info
.vite/
*.tsbuildinfo

# MSW service worker — generated (regenerated on install via msw.workerDirectory).
# Kept out of the repo so archives/zips don't carry a .js file that mail filters block.
public/mockServiceWorker.js
